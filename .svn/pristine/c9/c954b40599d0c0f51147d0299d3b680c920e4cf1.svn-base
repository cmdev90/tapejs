/*
 * Place all Model Associations in this script. To access Models use 
 * the Models args like so:
 *		Model.parent_model_name.hasOne(Model.child_Model_name);
 * 
 * For more information about writing associations in Sequelize.js visit
 *		|-> http://sequelizejs.com/documentation#associations
 *
 * Forigen Keys should also be defined here. More on that at:
 * 		|-> http://sequelizejs.com/documentation#associations-foreign-keys
 *
 */

module.exports = function (Model) {
	
	// Write your associations in here!

	// // Object_Type - Object relationship.
	// Model.Object_Type.hasMany(Model.Object, {foreignKeyConstraint: true});

	// Model.Object_Data.belongsTo(Model.Object, {foreignKeyConstraint: true});

	// Model.Object.hasMany(Model.Object_Data);

	// Model.Object_Data.belongsTo(Model.Object_Data_Types, {foreignKeyConstraint: true});

	// // The Object-Object relationship.
	// Model.Object.hasMany(Model.Object, {as: 'parent', foreignKeyConstraint: true});

	// // Indicator - Indicator_Templage relationship
	// Model.Indicator.belongsTo(Model.Indicator_Template, {foreignKeyConstraint: true});

	// // Indicator - Indicator_Data relationship
	// Model.Indicator.hasMany(Model.Indicator_Data, {foreignKeyConstraint: true});
};

 